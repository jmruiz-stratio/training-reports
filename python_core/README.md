# python_core

Núcleo reutilizable:
- Configuración y datasets (`config.py`).
- Cliente Moodle (`moodle_client.py`).
- Extracción RAW (`extraction.py`).
- Storage Parquet/HDFS (`storage/`).
- Validación técnica y sanity (`validation/`).
